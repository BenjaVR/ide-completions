export async function action() {
  // Type `axi` and it should complete and import `axios` from `axios`.
  // axi

  // Type `return named` and it should complete and import `namedAction` from `remix-utils/named-action`.
  // return named
}
